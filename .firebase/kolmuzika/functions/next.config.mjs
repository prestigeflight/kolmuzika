// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    devIndicators: {
      autoPrerender: false, // Disables automatic prerendering
    }
  }
  
  export default nextConfig;
  